name: Archived repository (Playwright disabled)

on:
  workflow_dispatch:

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This repository is archived; Playwright is disabled."
